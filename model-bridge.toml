[proxy]
host = "0.0.0.0"
port = 10010

[admin]
host = "0.0.0.0"
port = 10020

[database]
path = "model-bridge.db"

[bridge]
# 后台自动刷新模型列表间隔（分钟）
refresh_interval_min = 10

# Provider 定义 JSON 文件路径
providers_file = "providers.json"